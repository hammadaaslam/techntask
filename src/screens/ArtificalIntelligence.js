import {
    StyleSheet,
    Text,
    View,
    TextInput,
    SafeAreaView,
    Image,
    TouchableOpacity,
    ScrollView,
    StatusBar
  } from 'react-native';
  import React, { useState, useRef } from "react";


const ArtificalIntelligence = ({navigation}) => {
  return (
    <View style={{  flex: 1,
      //   justifyContent: 'center',
      //   alignItems: 'center',
      backgroundColor: 'white'}}>

<ScrollView>

          
              
             
                  </ScrollView>
  </View>
  )
}

export default ArtificalIntelligence

const styles = StyleSheet.create({})